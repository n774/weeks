<h1>Listing weeks</h1>

<table>
  <thead>
    <tr>
      <th>Employee</th>
      <th>Reported on</th>
      <th>Report</th>
      <th>Comment</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @weeks.each do |week| %>
      <tr>
        <td><%= week.employee_id %></td>
        <td><%= week.reported_on %></td>
        <td><%= week.report %></td>
        <td><%= week.comment %></td>
        <td><%= link_to 'Show', week %></td>
        <td><%= link_to 'Edit', edit_week_path(week) %></td>
        <td><%= link_to 'Destroy', week, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Week', new_week_path %>
